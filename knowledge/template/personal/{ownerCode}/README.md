# {{初始化:显示名称}}的个人知识

## 所有者

- 稳定标识：`{{初始化:ownerCode}}`
- 显示名称：{{初始化:姓名、昵称或团队内称呼}}

稳定标识用于目录归属，创建后不因姓名或昵称变化而修改。显示名称仅用于阅读，可以按实际情况更新。

## 目录定位

本目录只保存该所有者的个人经验、踩坑记录、临时理解和碎片素材，不代表项目或团队正式结论。

所有者目录只隔离内容归属，不提供访问控制。这里的内容会进入共享 Git 仓库，并可能被自动化工具读取或索引；不得写入密码、Token、私密通信、个人隐私或其他不应进入仓库的信息。

## 维护规则

- 只写来源于该所有者的个人内容，不把多人共同结论混入本目录。
- 个人内容需要复用时，先整理到 `knowledge/candidate/` 并补齐证据与验证条件。
- 按主题建立子目录时，每个子目录继续包含 README 与 INDEX；没有真实内容时不预造目录。
- 已退出个人有效经验路径但仍需追溯的内容，按原相对路径移动到 `knowledge/archive/personal/{{初始化:ownerCode}}/`。
