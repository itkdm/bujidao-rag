# 归档 applications

本目录接收原 `knowledge/applications/` 中退出当前有效路径的应用知识。

- 目标路径：`knowledge/archive/applications/<appCode>/<原 appCode 内相对路径>`；分类知识继续保留 `base/`、`feature/`、`rule/`、`tech/` 层级。
- 应用整体退休时可移动完整 appCode 目录；单文件归档时只创建必要层级。
- 保留 appCode 和分类相对路径，避免把不同应用的历史知识混在一起。
