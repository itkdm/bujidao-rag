# implemented

已实际实现并验证的 Change 放在本目录。

目录结构与 `proposed/` 一致。Change 完成后由 `proposed/` 移入：
`change.md` 语义从「计划」转为「实际决策」

- Proposal → Decision
- Acceptance → Verification
- Risks → Consequences

若后续失效或被新方案替代，移至 `../archived/`。
